services:
  pai:
    build: .
    image: pai:latest
    container_name: pai
    restart: unless-stopped
    ports:
      - "8788:8788"
    volumes:
      - pai-data:/app/data
    environment:
      # 可选：自定义服务监听端口（容器内）
      PORT: "8788"

volumes:
  pai-data:
